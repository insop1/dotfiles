# Dotfiles

Fedora 44 + Hyprland.

# Important

The waybar config uses [insop-media](https://github.com/Insop1/insop-media), you can either remove the custom module or build it into `~/.cargo/bin/`.

